enum PageState {
  DEFAULT,
  LOADING,
  SUCCESS,
  FAILED,
  UPDATED,
  CREATED,
  NO_INTERNET,
  MESSAGE,
  UNAUTHORIZED,
}

enum ButtonType { primary, secondary }

enum ButtonSize { full, half, trio, quarter, fifth, sixth, chips, auto }

enum ButtonRound { square, circle, rounded, cornered, halfRounded, quarterRounded, minRounded, microRound, auto }
